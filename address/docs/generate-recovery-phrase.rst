How to generate a recovery phrase
===============================================================================

Recovery phrases are stored in .prv files and used to restore a
private key. Here is an example that shows how to generate a new
recovery phrases.

.. literalinclude:: examples/generate-recovery-phrases.sh
   :language: bash
   :caption: Generates a recovery phrases.
   :name: addresses/generate-recovery-phrase.rst#code1

The command generates a recovery phrases in a file named phrase.prv.

